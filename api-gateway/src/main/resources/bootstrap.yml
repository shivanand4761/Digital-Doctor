spring:
    cloud:
        config:
            uri: http://${CONFIG_URI:${server_apigateway}}:8888
    application:
        name: apigatewayservice
    main:
        web-application-type: reactive
management:
    security:
        enabled: false
